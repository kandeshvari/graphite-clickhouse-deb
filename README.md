# graphite-clickhouse .deb pkg maker

## Build

Edit `Makefile` for actual `VERSION`

		# to build graphite-clickhouse binary
		make build
		
		# to build deb after binary 
		make build-deb
		
		# to build all
		make all
